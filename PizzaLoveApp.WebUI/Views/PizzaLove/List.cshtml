@model ProductListModel

<div class="row">
    <div class="col-md-12">
        <div class="row">
            @foreach (Product item in Model.Products)
            {
                <partial name="_product" for="@item" />
            }
        </div>
    </div>
</div>